"""loopeng — the workshop package.

Kept deliberately empty at scaffold time. The package exists so that every later
module is imported through one stable name (`loopeng.*`) rather than through
relative paths that shift when files move; a scaffold test asserts the import
works, so a broken src/ layout fails at test time rather than mid-demo.
"""
